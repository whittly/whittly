---
import Base from '../../../layouts/Base.astro';
import '../../../styles/account.css';

const API = import.meta.env.PUBLIC_API_URL ?? 'http://localhost:3001';
---

<Base title="новый пароль — whittly" description="Установите новый пароль для аккаунта whittly.">
  <div class="ac-auth-wrap">
    <div class="ac-auth-card">
      <div class="ac-auth-top">
        <div class="ac-auth-tag">// аккаунт</div>
        <h1 class="ac-auth-title">новый пароль</h1>
      </div>

      <div class="ac-auth-body">
        <div class="ac-error-msg" id="errMsg"></div>
        <div class="ac-success-msg" id="okMsg"></div>

        <div class="ac-field">
          <label for="password">новый пароль <span style="color:var(--ink-faint);font-size:10px;">(минимум 8 символов)</span></label>
          <input class="ac-input" id="password" type="password" autocomplete="new-password" placeholder="••••••••" />
        </div>

        <button class="ac-btn" id="submitBtn">сохранить пароль</button>
      </div>
    </div>
  </div>
</Base>

<script is:inline define:vars={{ API }}>
(function () {
  var passEl   = document.getElementById('password');
  var submitEl = document.getElementById('submitBtn');
  var errEl    = document.getElementById('errMsg');
  var okEl     = document.getElementById('okMsg');

  var token = new URLSearchParams(location.search).get('token');
  if (!token) {
    errEl.textContent = 'неверная или отсутствующая ссылка сброса';
    errEl.classList.add('visible');
    submitEl.disabled = true;
  }

  function showError(msg) { errEl.textContent = msg; errEl.classList.add('visible'); }
  function showOk(msg)    { okEl.textContent  = msg; okEl.classList.add('visible'); }

  async function submit() {
    errEl.classList.remove('visible');
    var password = passEl.value;
    if (password.length < 8) { showError('пароль должен содержать минимум 8 символов'); return; }

    submitEl.disabled = true;
    submitEl.textContent = 'сохраняем…';

    try {
      var res  = await fetch(API + '/auth/reset-password', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ token, password }),
      });
      var data = await res.json();

      if (!res.ok) { showError(data.error ?? 'что-то пошло не так'); submitEl.disabled = false; submitEl.textContent = 'сохранить пароль'; return; }

      showOk('пароль обновлён — переходим к входу…');
      setTimeout(function () { location.replace('/ru/account/login/'); }, 2000);
    } catch {
      showError('сервер недоступен — попробуй позже');
      submitEl.disabled = false;
      submitEl.textContent = 'сохранить пароль';
    }
  }

  submitEl.addEventListener('click', submit);
  passEl.addEventListener('keydown', function (e) { if (e.key === 'Enter') submit(); });
})();
</script>
